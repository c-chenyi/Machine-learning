- In most applications, the relevant measure of model quality is predictive accuracy.
- MAE: Mean Absolute Error
- validation data: Exclude some data from the model-building process and then use those to test the moodel' accuracy on data it hasn't seen before.

```python
from sklearn.tree import DecisionTreeRegressor
from sklearn.metrics import mean_absolute_error
from sklearn.model_selection import train_test_split

melbourne_model = DecisionTreeRegressor(random_state=1)
melbourne_model.fit(train_X, train_y)

predicted_home_prices = melbourne_medel.precit(X)
mean_absolute_error(y, predicted_home_prices)

# 将数据分成两部分：training data 和 validation data
train_X, val_X, train_y, val_y = train_test_split(X, y, random_state=1)
```

Last: [[3.Your First Machine Learning Model]]
Next: [[5.Underfitting and Overfitting]]